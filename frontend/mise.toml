[task_config]
includes = ["../mise.tasks.toml"]

[tasks.dev]
description = "Start frontend dev server"
run = "npm run dev 2>&1 | tee /tmp/frontend.log"

[tasks.test]
run = "npm test"

[tasks.clean]
description = "Remove build artifacts"
run = "rm -rf build"
